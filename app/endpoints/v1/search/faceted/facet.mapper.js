// app/endopints/v1/search/faceted/facet.mapper


/*
	Logic for mapping preference tuples will go here.

	This section is incomplete because the query DSL for the system
	  is not yet defined.
	Thus, I allow any kind of query through. This should not be left in,
	  as it could allow access to hidden fields or secrets.
*/

module.exports = function(facetedQuery) {

	// STUBBED...
	return facetedQuery;

};
